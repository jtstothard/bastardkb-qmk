---
phase: 09-testing-and-validation
plan: 04
type: execute
wave: 4
depends_on: ["01", "02", "03"]
files_modified: []
autonomous: true
---

<objective>
Test EEPROM persistence across reboots for all VIA settings.

Purpose: Validate all 30+ VIA settings persist correctly after power cycling and factory reset works as expected.
Output: Test results documenting EEPROM durability, persistence accuracy, and factory reset functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@keyboards/bastardkb/dilemma/dilemma.h
@keyboards/bastardkb/dilemma/dilemma.c
</context>

<tasks>

<task type="manual">
  <name>Task 1: Test DPI settings persistence (presets, custom, sniping)</name>
  <action>
Test EEPROM persistence for all DPI-related settings:

**Test Procedure:**
1. Open VIA configurator → "Pointer" section
2. Set all DPI settings to non-default values:
   - DPI Preset = 3 (800 DPI)
   - Custom DPI = 1600
   - Sniping DPI = 2 (600 DPI)
3. Click "Save" button
4. Power cycle Dilemma v3 (unplug USB, wait 5 seconds, replug)
5. Reopen VIA configurator
6. Verify all DPI values match saved settings
7. Test pointer behavior to confirm values applied

**Expected Results:**
- DPI preset value persists (3 = 800 DPI)
- Custom DPI value persists (1600)
- Sniping DPI preset persists (2 = 600 DPI)
- VIA shows correct values after reconnect
- Pointer speed matches saved DPI settings on boot
- No EEPROM corruption or value drift

**Validation:**
- [ ] DPI preset persists across power cycle (value = 3)
- [ ] Custom DPI persists across power cycle (value = 1600)
- [ ] Sniping DPI persists across power cycle (value = 2)
- [ ] VIA shows correct values after reconnect
- [ ] Pointer speed matches saved settings on boot
- [ ] No value corruption or bit rot after power cycle

**Additional Tests:**
- Perform 10 power cycles in succession
- Check for any EEPROM degradation or value changes
- Test extreme DPI values: 200 (min), 4000 (max)

**Record Results:**
- Document DPI values before and after each power cycle
- Note any value drift or corruption
- Verify EEPROM durability over multiple cycles
  </action>
  <verify>Test log shows DPI persistence test results with power cycle validation</verify>
  <done>DPI settings persistence verified (all 3 values preserved after power cycles)</done>
</task>

<task type="manual">
  <name>Task 2: Test scroll divisor persistence (all 4 divisors)</name>
  <action>
Test EEPROM persistence for all scroll divisor settings:

**Test Procedure:**
1. Open VIA configurator → "Scroll" section
2. Set all 4 scroll divisors to non-default values:
   - Drag Scroll X Divisor = 7
   - Drag Scroll Y Divisor = 11
   - Two-Finger Scroll X Divisor = 5
   - Two-Finger Scroll Y Divisor = 9
3. Click "Save" button
4. Power cycle Dilemma v3 (unplug USB, wait 5 seconds, replug)
5. Reopen VIA configurator
6. Verify all divisor values match saved settings
7. Test scroll behavior in both modes to confirm values applied

**Expected Results:**
- Drag Scroll X persists (value = 7)
- Drag Scroll Y persists (value = 11)
- Two-Finger Scroll X persists (value = 5)
- Two-Finger Scroll Y persists (value = 9)
- VIA shows correct values after reconnect
- Scroll speeds match saved divisor settings on boot
- No EEPROM corruption or cross-mode interference

**Validation:**
- [ ] Drag Scroll X persists across power cycle
- [ ] Drag Scroll Y persists across power cycle
- [ ] Two-Finger Scroll X persists across power cycle
- [ ] Two-Finger Scroll Y persists across power cycle
- [ ] VIA shows correct values after reconnect
- [ ] Scroll speeds match saved settings on boot
- [ ] No cross-mode interference (drag vs two-finger)

**Additional Tests:**
- Perform 10 power cycles in succession
- Test asymmetric divisors (e.g., X=1, Y=15)
- Verify mode switching doesn't corrupt divisors

**Record Results:**
- Document divisor values before and after each power cycle
- Note any cross-mode interference or corruption
- Verify EEPROM durability for all 4 divisors
  </action>
  <verify>Test log shows scroll divisor persistence test results with all 4 divisors validated</verify>
  <done>Scroll divisor persistence verified (all 4 values preserved after power cycles)</done>
</task>

<task type="manual">
  <name>Task 3: Test gesture enable and keycode persistence</name>
  <action>
Test EEPROM persistence for all gesture enables and keycodes:

**Test Procedure Part A - Gesture Enables:**
1. Open VIA configurator → "Gestures - Basic" and "Gestures - Advanced"
2. Toggle all gesture enables to specific states:
   - Tap to Click = OFF (test disabled state)
   - Two-Finger Tap = ON
   - Two-Finger Scroll = ON
   - Press and Hold = ON
   - Three-Finger Swipe = ON
   - Four-Finger Swipe = ON
   - Pinch to Zoom = OFF (test disabled state)
3. Click "Save" button
4. Power cycle Dilemma v3
5. Verify all toggle states preserved

**Test Procedure Part B - Gesture Keycodes:**
1. Navigate to "Swipe Keycodes" and "Gesture Keycodes"
2. Set all 13 gesture keycodes to unique test values:
   - All swipe direction keycodes (8): Set to KC_1 through KC_8
   - Zoom In/Out keycodes (2): Set to KC_9 and KC_0
3. Click "Save" button
4. Power cycle Dilemma v3
5. Verify all keycode values preserved

**Expected Results:**
- All 7 gesture enable toggles persist (ON/OFF states)
- All 13 gesture keycodes persist (custom values)
- VIA shows correct values after reconnect
- Gesture behavior matches saved settings on boot
- No EEPROM corruption or keycode conflicts

**Validation:**
- [ ] All 7 gesture enables persist (toggle states)
- [ ] All 8 swipe keycodes persist (custom values)
- [ ] Zoom in/out keycodes persist (custom values)
- [ ] Basic gesture keycodes persist (if editable)
- [ ] VIA shows correct values after reconnect
- [ ] Gesture behavior matches saved settings
- [ ] No keycode corruption or conflicts

**Additional Tests:**
- Test disabled gestures don't trigger after power cycle
- Verify keycode 0 (undefined) persists correctly
- Test extreme keycode values: 0x0000, 0xFFFF

**Record Results:**
- Document all gesture enable states and keycodes before/after power cycle
- Verify disabled gestures stay disabled
- Note any keycode corruption or conflicts
  </action>
  <verify>Test log shows gesture settings persistence test results with enables and keycodes</verify>
  <done>Gesture settings persistence verified (all enables and keycodes preserved)</done>
</task>

<task type="manual">
  <name>Task 4: Test advanced settings persistence (auto-snipe, force click)</name>
  <action>
Test EEPROM persistence for advanced feature settings:

**Test Procedure Part A - Auto-Snipe Settings:**
1. Open VIA configurator → "Advanced Settings"
2. Set auto-snipe configuration:
   - Auto-Snipe Enable = ON
   - Auto-Snipe Layer = 5
3. Click "Save" button
4. Power cycle Dilemma v3
5. Verify auto-snipe settings preserved

**Test Procedure Part B - Smart Gesture Features:**
1. Verify force click settings persist (uses press_and_hold_keycode from Phase 3)
2. Verify smart zoom settings persist (pinch_to_zoom_enabled from Phase 4)
3. Test that advanced feature behavior matches saved settings after power cycle

**Expected Results:**
- Auto-snipe enable persists (ON/OFF state)
- Auto-snipe layer persists (value = 5)
- Force click keycode persists (via press_and_hold_keycode)
- Smart zoom enable persists (via pinch_to_zoom_enabled)
- Advanced features work correctly after power cycle
- No EEPROM corruption for advanced settings

**Validation:**
- [ ] Auto-snipe enable persists across power cycle
- [ ] Auto-snipe layer persists across power cycle
- [ ] Force click keycode persists (indirectly via press_and_hold)
- [ ] Smart zoom enable persists (indirectly via pinch_to_zoom)
- [ ] VIA shows correct values after reconnect
- [ ] Advanced features work correctly on boot
- [ ] No EEPROM corruption for advanced settings

**Additional Tests:**
- Test auto-snipe functionality after power cycle (enter layer 5, verify sniping enables)
- Test force click gesture after power cycle
- Test pinch-to-zoom after power cycle

**Record Results:**
- Document advanced settings before/after power cycle
- Verify advanced features work correctly after reboot
- Note any EEPROM corruption or bugs
  </action>
  <verify>Test log shows advanced settings persistence test results with auto-snipe and smart features</verify>
  <done>Advanced settings persistence verified (auto-snipe, force click, smart zoom preserved)</done>
</task>

<task type="manual">
  <name>Task 5: Test factory reset and EEPROM durability</name>
  <action>
Test factory reset functionality and EEPROM write endurance:

**Test Procedure Part A - Factory Reset:**
1. Configure all VIA settings to custom values (DPI, scroll, gestures, advanced)
2. Navigate to VIA "Actions" → "Reset to Defaults"
3. Click reset button
4. Verify all settings return to default values:
   - DPI preset = 0 (200 DPI, default)
   - Custom DPI = 800 (default)
   - Scroll divisors = 8 (default medium)
   - All gesture enables = ON (default)
   - Auto-snipe = OFF (default)
5. Test device behavior to confirm defaults applied

**Test Procedure Part B - EEPROM Write Endurance:**
1. Configure all settings to custom values
2. Click "Save" button 50 times in succession (stress test)
3. Power cycle after each 10 saves
4. Verify no EEPROM corruption or value drift

**Test Procedure Part C - EEPROM Full Layout Test:**
1. Fill entire 64-byte VIA config with non-default values
2. Power cycle and verify all bytes persist
3. Check for any bit flips or corruption

**Expected Results:**
- Factory reset restores all default values
- No EEPROM corruption after 50 save cycles
- All 64 bytes persist correctly when fully utilized
- No bit flips or corruption in EEPROM data

**Validation:**
- [ ] Factory reset restores all default values
- [ ] All settings return to firmware defaults after reset
- [ ] Device behavior matches defaults after reset
- [ ] No EEPROM corruption after 50 save cycles
- [ ] No value drift or bit flips after repeated saves
- [ ] All 64 bytes persist when fully utilized
- [ ] EEPROM write endurance within acceptable limits

**Record Results:**
- Document default values after factory reset
- Note any EEPROM corruption after stress testing
- Verify EEPROM durability for production use
  </action>
  <verify>Test log shows factory reset and EEPROM durability test results</verify>
  <done>Factory reset and EEPROM durability verified (all settings reset correctly, no corruption)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 test tasks executed on Dilemma v3 hardware
- [ ] DPI settings persistence tested (presets, custom, sniping)
- [ ] Scroll divisor persistence tested (all 4 divisors)
- [ ] Gesture settings persistence tested (enables and keycodes)
- [ ] Advanced settings persistence tested (auto-snipe, smart features)
- [ ] Factory reset and EEPROM durability tested
- [ ] Test results documented in SUMMARY.md
- [ ] Any EEPROM corruption or failures logged with details
- [ ] VIA configurator integration verified for all settings
</verification>

<success_criteria>

- All VIA settings (30+) tested for EEPROM persistence
- Factory reset functionality validated
- EEPROM durability confirmed for production use
- Test results documented with pass/fail for each setting
- Any EEPROM issues or bugs identified and logged in STATE.md
- Ready for Plan 09-05 (VIA menu navigation testing)
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-and-validation/09-04-SUMMARY.md`
</output>
