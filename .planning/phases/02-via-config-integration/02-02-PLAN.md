---
phase: 02-via-config-integration
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: [keyboards/bastardkb/dilemma/dilemma.c]
autonomous: true
---

<objective>
Implement scroll divisor configuration to apply VIA scroll settings to the MaxTouch digitizer.

Purpose: Enable separate scroll speeds for drag-scroll vs two-finger scroll by applying VIA config divisors to the digitizer driver.

Output: Scroll divisor application logic that respects VIA config settings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-via-config-integration/02-01-SUMMARY.md

@keyboards/bastardkb/dilemma/dilemma.h
@keyboards/bastardkb/dilemma/dilemma.c
@quantum/digitizer_mouse_fallback.c
@drivers/sensors/maxtouch.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research scroll divisor usage in digitizer driver</name>
  <files>quantum/digitizer_mouse_fallback.c</files>
  <action>
  Read quantum/digitizer_mouse_fallback.c to understand:
  1. How DIGITIZER_SCROLL_DIVISOR is currently used
  2. Where scroll divisor is applied (look for h / DIGITIZER_SCROLL_DIVISOR)
  3. Whether the divisor is a compile-time constant or runtime variable
  4. The data structure used for mouse reporting (report_mouse_t)

  From earlier grep, we know:
  - Line 259-260: carry_h = h % DIGITIZER_SCROLL_DIVISOR
  - Line 262-263: mouse_report.h = h / DIGITIZER_SCROLL_DIVISOR

  Determine if we need to:
  - Add runtime variable support to digitizer driver, OR
  - Apply divisor at dilemma.c level before passing to driver

  This task is analysis only - no code changes.
  </action>
  <verify>Confirmed understanding of scroll divisor application point</verify>
  <done>Clear plan for where to apply VIA scroll divisors</done>
</task>

<task type="auto">
  <name>Task 2: Add global divisor variables to dilemma.c</name>
  <files>keyboards/bastardkb/dilemma/dilemma.c</files>
  <action>
  Add two global variables to track current scroll divisors:
  ```c
  static uint16_t g_current_scroll_x_divisor = 8;  // Default
  static uint16_t g_current_scroll_y_divisor = 8;  // Default
  ```

  These will be used by pointing_device_task_dilemma() to apply scrolling.
  The divisor values come from g_via_dilemma_config fields:
  - drag_scroll_x_divisor (drag-scroll mode)
  - drag_scroll_y_divisor (drag-scroll mode)
  - two_finger_x_divisor (two-finger scroll)
  - two_finger_y_divisor (two-finger scroll)

  Initialize with default values matching VIA config defaults (8).
  </action>
  <verify>Variables compile and are accessible in dilemma.c scope</verify>
  <done>Global divisor tracking variables exist with correct defaults</done>
</task>

<task type="auto">
  <name>Task 3: Update pointing_device_task_dilemma() to use VIA divisors</name>
  <files>keyboards/bastardkb/dilemma/dilemma.c</files>
  <action>
  Modify the existing pointing_device_task_dilemma() function (lines 196-221) to:
  1. Update divisor globals based on current mode before processing scroll:
     - If is_dragscroll_enabled: use drag_scroll_x/y_divisor from VIA config
     - If NOT is_dragscroll_enabled: use two_finger_x/y_divisor from VIA config
  2. Change scroll_buffer calculation to use divisor variables instead of hardcoded buffer size
  3. Keep existing DILEMMA_DRAGSCROLL_BUFFER_SIZE logic for scroll threshold

  The divisor scales the mouse movement before scroll accumulation:
  - Input: mouse_report->x (raw movement from trackpad)
  - Divide by g_current_scroll_x_divisor
  - Accumulate in scroll_buffer_x
  - When buffer > threshold, emit scroll event

  This allows different scroll speeds for drag vs two-finger modes.
  </action>
  <verify>Compile completes, scroll logic is functionally equivalent with new divisor application</verify>
  <done>Scroll divisor is now configurable via VIA config and applied at runtime</done>
</task>

<task type="auto">
  <name>Task 4: Add divisor update function</name>
  <files>keyboards/bastardkb/dilemma/dilemma.c</files>
  <action>
  Create a function `update_scroll_divisors()` that:
  1. Checks g_dilemma_config.is_dragscroll_enabled
  2. If dragscroll enabled: sets g_current_scroll_x/y_divisor from g_via_dilemma_config.drag_scroll_x/y_divisor
  3. If dragscroll disabled: sets g_current_scroll_x/y_divisor from g_via_dilemma_config.two_finger_x/y_divisor
  4. Returns void

  Call this function from:
  - pointing_device_task_dilemma() on each iteration (cheap, keeps divisor in sync)
  - dilemma_set_pointer_dragscroll_enabled() after mode change
  - apply_via_dilemma_config() after VIA config changes

  This ensures divisors update when mode changes or VIA config is saved.
  </action>
  <verify>Function is called from all three locations</verify>
  <done>Scroll divisors update automatically when mode or config changes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Code compiles without errors
- [ ] Scroll divisor variables track current mode correctly
- [ ] Drag-scroll uses drag_scroll_x/y_divisor from VIA config
- [ ] Two-finger scroll uses two_finger_x/y_divisor from VIA config
- [ ] Divisors update when drag-scroll mode toggles
- [ ] No breaking changes to existing scroll functionality
</verification>

<success_criteria>

- Scroll divisors are configurable via VIA
- Separate divisors for drag-scroll and two-finger scroll
- Divisors update automatically on mode changes
- Scroll speed is now user-adjustable without firmware rebuild
</success_criteria>

<output>
After completion, create `.planning/phases/02-via-config-integration/02-02-SUMMARY.md`
</output>
